#include <node.h>

class LinkedListRoot {
  public:
     int PARENT;
     void forEach(int);
     int every(int);
  
}
void LinkedListRoot::forEach(int) {
  
}
int LinkedListRoot::every(int) {
  
  return 1;
}


class LinkedListNode {
  public:
     int PARENT;
     int CHILD;
     void forEach(int);
     int every(int);
     void destroy(void)
  
}
void LinkedListRoot::forEach(int) {
  
}
int LinkedListRoot::every(int) {
  
  return 1;
}
int
